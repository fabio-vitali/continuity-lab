# MI-001E-R1 — Superseding Candidate Genuine Claude Code Invocation Confirmation Correction

Run MI-001E-R1 only when the committed Context Pack status is
`authorized-not-started`, all publication bindings pass, and a separate
explicit user instruction orders execution. Contract review, authorization,
commit, or push alone does not start execution.

The finalized Nestfolio execution binding is
`c5a54d04fe1d88e36fd6e466b62ae1e4d468d87a`. Before reading repository
content, verify by Git metadata that continuity-lab is clean and pushed at the
exact final authorization revision and Nestfolio is clean and pushed at this
exact revision. Read Program State first. If any binding or authority conflicts,
stop before execution and report the contradiction without issuing an R1
result.

After a later separate user instruction expressly orders execution, run
MI-001E-R1 exactly as follows:

> Execute MI-001E-R1 exactly from its final committed authorized contract. Read
> Program State first after Git-metadata verification. Require clean pushed
> continuity-lab and Nestfolio revisions equal to the final contract bindings.
> Execute exactly two fresh genuine Claude Code Sessions and only the contracted
> `/backlog-next --auto` scenarios. Machine-capture both exact process intervals
> before launch and after exit. Keep all R1 evidence outside both repositories
> until both Sessions finish and restoration passes. For Scenario B, use only
> the contract's single-path temporary skip-worktree containment for the exact
> disabled activation file, prove disabled bytes and clean current-work status,
> then clear the bit and restore exact active 1.0.1 before publishing evidence.
> Do not execute the selected backlog item, modify the candidate or Skill,
> change implementation, recover MI-001 output, authorize MI-002, or commit or
> push during execution.

The future executor must then follow the complete Context Pack, not this short
instruction alone. Any unresolved revision, authorization, clean-state,
executor, timing, containment, restoration, or scope condition stops before
execution or produces the unconditional result required by the contract.
