---
artifact_id: SESSION-PROMPT-SE-001
status: provisional
owner: session
last_updated: 2026-07-16
supersedes: []
superseded_by: null
---

# SE-001 — Preserved Future Execution Instruction

This file is not execution authorization. It becomes usable only after the
reviewed contract is committed and pushed unchanged and a later user
instruction supplies the exact published continuity-lab revision, repeats all
three exact bindings, and explicitly authorizes only SE-001 execution. The
session executor is Claude Code operating directly under that instruction.

```text
This is a fresh SE-001 — Selected Effort Execution control session only. It
authorizes bounded execution of the unchanged contract at continuity-lab
revision <EXACT-PUBLISHED-SE-001-CONTRACT-REVISION>: implementing exactly the
selected backlog effort dashboard-bff-awaiting-confirmation-activity-gap
under the digest-matched adapter view of the authorized Context Pack
nestfolio.context.dashboard-bff-awaiting-confirmation-activity-gap version 1,
SHA-256 e58c9bc1978d6799cccda96c2520ea1f7f41ab8b88e8a055ff968cd7b8ce15c1. The
session executor is Claude Code operating directly under this instruction;
conducting the contracted work directly does not constitute invoking Claude
Code. It does not authorize publication, commit, push, MI-005, broader work,
nested executors or sub-agents, any Skill or slash command, dependency
changes, external writes, or unrelated repository work. The harness-injected
continuity-workspace/CLAUDE.md is part of the session environment and is not
an executor-initiated content read.

Do not rely on previous chat state.

Before reading repository content, verify Git metadata separately for
continuity-workspace, continuity-lab, and Nestfolio. Require clean main
branches with HEAD, local origin/main, upstream, and remote main exactly
aligned at continuity-workspace 56e10aa92429a8ae793463d1cd1c7ad9542e7f64,
continuity-lab <EXACT-PUBLISHED-SE-001-CONTRACT-REVISION>, and Nestfolio
a760d6f41ff14491d5b008a7f13234ba0221bcec. Require the continuity-lab revision
to contain the unchanged SE-001 contract byte-for-byte, exact subject
"Authorize SE-001 selected effort execution", and exact single parent
1b5c4a775ee6673509d1acd5f5676ffd80387dac. Stop before content read on any
contradiction. Do not fetch, pull, merge, rebase, reset, checkout, restore,
stash, clean, or rewrite history.

After every gate passes, open continuity-lab docs/90-state/program-state.md
as the first logical repository path and keep it the sole logical path
addressed until its complete-read proof closes. Follow
canonical_complete_read_protocol and the exact path order in the SE-001
contract. For every path, record its bound blob identity, byte count,
logical-line count, and whole-path SHA-256, then read it completely in
contiguous, ordered, forward-only chunks with the file-read tool. Credit a
chunk only when the response presents the requested range with no truncation,
clipping, omission, elision, or persisted-to-file substitution; anything else
is CANONICAL_SEGMENT_INCOMPLETE and may be recovered only by a
forward-contiguous re-read within the same open path session. Close each path
only on gapless full-line coverage plus final byte, SHA-256, and blob
verification. Never batch, parallelize, interleave, reorder, skip, or
substitute a path.

Preserve MI-002-R2 PASS, MI-003 FAIL, MI-003-R1 FAIL, and MI-003-R2 PASS as
four separate immutable results, MI-004 PASS as a separately recorded result,
and every historical artifact byte. Treat every published Level 1-4 artifact,
the authorized Context Pack version 1, its authorization record, its adapter
view, and the backlog source at the bound Nestfolio revision as read-only
inputs; verify every bound digest before any mutation and block fail-closed
on any mismatch, staleness, rejection, or revocation.

Consume the effort exclusively through the digest-matched adapter view.
Derive the writable boundary deterministically from the published Level 3
scope.json and working-set.json as carried by the authorized Context Pack,
prove three byte-identical rebuilds, freeze and record it before the first
mutation, and reject any boundary path that violates the contract's
boundary_scope_rule. Prove the awaiting-confirmation activity gap with the
contracted fail-before/pass-after validation: the validation fails in an
isolated temporary copy at the exact start revision and passes in the
implemented working tree.

Mutate only recorded writable-boundary paths and the exact fifteen SE-001
evidence create paths. Keep every other pre-existing tracked byte immutable,
including the backlog source and every Level 1-4 artifact. Run every
contracted success, mandatory-failure, rebuild, return/cancel, preservation,
lower-level regression, and exact-manifest validation; Level 4, Level 3,
Level 2, retained Level 1, and backlog-next suites must return 23/23, 23/23,
23/23, 14/14, and 68/68 after implementation without invoking any Skill. Use
no dependency operation. Do not spawn any nested executor, sub-agent, Skill,
or slash command. Record no completion, Run, Assurance, or Level 5-6 state.

Apply the conjunctive unconditional verdict. After an actual mutation
failure, restore every writable-boundary path to its recorded pre-change
bytes, remove only newly created SE-001 evidence paths, restore and prove the
exact clean Nestfolio start state, retain diagnostics outside all
repositories, and close only through the exact SE-001 continuity-lab
execution-closure scope. Leave the execution delta uncommitted and unstaged
with a recorded SHA-256 manifest; publication of the implementation requires
a separate later authorization with the exact subject "Publish SE-001
selected effort implementation and evidence". Do not commit or push.

Report the SE-001 result, immutable historical disposition, every canonical
path and coverage proof, context-delivery digests, the frozen writable
boundary, the gap proof, C1-C7, S1-S10, F1-F12, validation, preservation,
rollback, exact manifests, and next valid operation. No partial or qualified
PASS is permitted.
```

The placeholder must be replaced by the exact published contract revision in
the later explicit execution instruction. Leaving it unresolved is a hard
stop.
