---
artifact_id: SESSION-PROMPT-MI-004
status: provisional
owner: session
last_updated: 2026-07-16
supersedes: []
superseded_by: null
---

# MI-004 — Preserved Future Execution Instruction

This file is not execution authorization. It becomes usable only after the
reviewed contract is committed and pushed unchanged and a later user
instruction supplies the exact published continuity-lab revision, repeats all
three exact bindings, and explicitly authorizes only MI-004 execution. The
session executor is Claude Code operating directly under that instruction.

```text
This is a fresh MI-004 — Authorized Context Introduction execution control
session only. It authorizes bounded execution of the unchanged contract at
continuity-lab revision <EXACT-PUBLISHED-MI-004-CONTRACT-REVISION>. The
session executor is Claude Code operating directly under this instruction;
conducting the contracted work directly does not constitute invoking Claude
Code. It does not authorize publication, implementation of the selected
backlog effort, MI-005, broader work, nested executors or sub-agents, any
Skill or slash command, dependency changes, external writes, commit, push, or
unrelated repository work. The harness-injected continuity-workspace/CLAUDE.md
is part of the session environment and is not an executor-initiated content
read.

Do not rely on previous chat state.

Before reading repository content, verify Git metadata separately for
continuity-workspace, continuity-lab, and Nestfolio. Require clean main
branches with HEAD, local origin/main, upstream, and remote main exactly
aligned at continuity-workspace 56e10aa92429a8ae793463d1cd1c7ad9542e7f64,
continuity-lab <EXACT-PUBLISHED-MI-004-CONTRACT-REVISION>, and Nestfolio
b9d7264082322e09cfd233819b79f128ef912e31. Require the continuity-lab revision
to contain the unchanged MI-004 contract byte-for-byte, exact subject
"Authorize MI-004 authorized context introduction", and exact single parent
5519ea78b7a093678c2ed87a6c310b20363e28b6. Stop before content read on any
contradiction. Do not fetch, pull, merge, rebase, reset, checkout, restore,
stash, clean, or rewrite history.

After every gate passes, open continuity-lab docs/90-state/program-state.md as
the first logical repository path and keep it the sole logical path addressed
until its complete-read proof closes. Follow canonical_complete_read_protocol
and the exact path order in the MI-004 Context Pack. For every path, record
its bound blob identity, byte count, logical-line count, and whole-path
SHA-256, then read it completely in contiguous, ordered, forward-only chunks
with the file-read tool. Credit a chunk only when the response presents the
requested range with no truncation, clipping, omission, elision, or
persisted-to-file substitution; anything else is CANONICAL_SEGMENT_INCOMPLETE
and may be recovered only by a forward-contiguous re-read within the same open
path session. Close each path only on gapless full-line coverage plus final
byte, SHA-256, and blob verification. Never batch, parallelize, interleave,
reorder, skip, or substitute a path.

Preserve MI-002-R2 PASS, MI-003 FAIL, MI-003-R1 FAIL, and MI-003-R2 PASS as
four separate immutable results and every historical artifact byte. Treat the
published Level 3 artifacts and active route at the bound Nestfolio revision
as read-only inputs; verify their internal digest consistency and the bound
selected-effort source digest before any mutation.

Form the Context Pack only through the explicit DR-0011 pipeline recorded in
the contract, over the declared read-only sources, with a complete source and
exclusion ledger, recorded gaps and contradictions, and deterministic
rebuild. Validate the exact candidate digest. Then present the validated
Context Pack identity, version, digest, ledger summary, and gaps, and stop.
Do not authorize automatically. Require the user to explicitly authorize
exactly one validated version by identity, version, and exact SHA-256 digest
in a later message; absence, multiplicity, or mismatch blocks with
CONTEXT_AUTHORIZATION_MISSING. Authorization authorizes only the Level 4
context authority and digest-matched adapter view; never perform the selected
effort and never create Level 5-6 state.

Create only the exact twenty-two permitted Nestfolio additions. Keep every
pre-existing tracked byte immutable, including every Level 3 artifact and the
backlog source. Run every contracted success, mandatory-failure, rebuild,
rejection/revocation/staleness, preservation, lower-level regression, and
exact-manifest validation. Use no dependency operation. Do not spawn any
nested executor, sub-agent, Skill, or slash command.

Apply the conjunctive unconditional verdict. After an actual mutation failure,
remove only newly created MI-004 paths, restore and prove exact clean
Nestfolio start state, retain diagnostics outside all repositories, and close
only through the exact MI-004 continuity-lab execution-closure scope. Do not
commit or push.

Report the MI-004 result, immutable historical disposition, every canonical
path and coverage proof, formation and validation identity, the exact human
authorization tuple, C1-C7, S1-S10, F1-F12, validation, preservation,
rollback, exact manifests, and next valid operation. No partial or qualified
PASS is permitted.
```

The placeholder must be replaced by the exact published contract revision in
the later explicit execution instruction. Leaving it unresolved is a hard
stop.
