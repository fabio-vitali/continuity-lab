---
artifact_id: SESSION-PROMPT-MI-003-R2
status: provisional
owner: session
last_updated: 2026-07-15
supersedes: []
superseded_by: null
---

# MI-003-R2 — Preserved Future Execution Instruction

This file is not execution authorization. It becomes usable only after the
finalized contract is committed and pushed unchanged and a later user
instruction supplies the exact published continuity-lab revision, repeats all
three exact bindings, and explicitly authorizes only MI-003-R2 execution.

```text
This is a fresh MI-003-R2 — Bounded Local Work and Scope Segmented Canonical
Complete-Read Correction Retry execution control session only. It authorizes
bounded execution of the unchanged finalized contract at continuity-lab
revision <EXACT-PUBLISHED-MI-003-R2-CONTRACT-REVISION>. It does not authorize
publication, implementation of a selected backlog effort, MI-004, broader work,
Claude Code, any Skill, dependency changes, external writes, commit, push, or
unrelated repository work.

Do not rely on previous chat state.

Before reading repository content, verify Git metadata separately for
continuity-workspace, continuity-lab, and Nestfolio. Require clean main branches
with HEAD, local origin/main, upstream, and remote main exactly aligned at
continuity-workspace eb990e114973e42af9935379f6b13f34f2e5e3b8,
continuity-lab <EXACT-PUBLISHED-MI-003-R2-CONTRACT-REVISION>, and Nestfolio
fd5e58b35e665fa5fa0db3c2c31ea5561442f9b6. Require the continuity-lab revision
to contain the unchanged finalized R2 contract, exact subject "Authorize
MI-003-R2 segmented canonical complete-read correction retry", exact single
parent 323834ff98bdf066daaf96d24957e154c899393e, and exactly the reviewed
nine-path delta. Stop before content read on any contradiction. Do not fetch,
pull, merge, rebase, reset, checkout, restore, stash, clean, or rewrite history.

After every gate passes, open continuity-lab
docs/90-state/program-state.md as the first logical repository path and keep it
the sole logical path addressed until its complete-read proof closes. Follow
canonical_complete_read_protocol and the exact path order in the R2 Context
Pack. For every path, establish its bound blob, byte count, logical-line count,
whole-path SHA-256, and greedy-utf8-codepoints-max-4096-v1 segment plan. Emit
exactly one contiguous readable UTF-8 segment per response with matching begin
and end envelopes and a tool allowance of at least 12000 tokens. Advance only
when the response explicitly reports no truncation, clipping, omission, or
elision and the payload bytes, offsets, line cursors, and SHA-256 verify. Never batch,
parallelize, interleave, reorder, skip, resize, retry, or substitute a segment
or path. Any incomplete output is CANONICAL_SEGMENT_INCOMPLETE and cannot count
as completion.

Preserve MI-003 and MI-003-R1 as separate immutable UNCONDITIONAL FAIL results.
Preserve every original and R1 artifact and all MI-002 history byte-for-byte.
Execute only the unchanged MI-003 objective, human-selection boundary,
Nestfolio scope, S1-S10, F1-F12, validation, rollback, prohibitions, and
conjunctive verdict retained by the R2 Context Pack.

First derive the complete deterministic candidate projection and exact
inclusion/exclusion rationale. Do not select silently. If this instruction does
not contain an exact human selection tuple, stop and request exactly one
candidate source path, stable source-local identity, source revision, and
source SHA-256, then verify an exact projection match. Selection authorizes only
the Level 3 representation and route; never perform the selected effort.

Create only the exact twenty-two permitted Nestfolio additions. Keep every
pre-existing tracked byte immutable. Run every contracted success,
mandatory-failure, rebuild, return/cancel, preservation, lower-level regression,
and exact-manifest validation. Use no dependency operation. Do not invoke
Claude Code or any Skill. Do not create Level 4-6 authority or external writes.

Apply the conjunctive unconditional verdict. After an actual mutation failure,
remove only newly created MI-003 paths, restore and prove exact clean Nestfolio
start state, retain diagnostics outside all repositories, and close only through
the exact R2 continuity-lab execution-closure scope. Do not commit or push.

Report the R2 result, immutable historical disposition, every canonical path
and segment proof, candidate and human-selection identity, C1-C7, S1-S10,
F1-F12, validation, preservation, rollback, exact manifests, and next valid
operation. No partial or qualified PASS is permitted.
```

The placeholder must be replaced by the exact published R2 contract revision in
the later explicit execution instruction. Leaving it unresolved is a hard stop.
