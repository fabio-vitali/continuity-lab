---
artifact_id: SESSION-PROMPT-MI-003-R2
status: provisional
owner: session
last_updated: 2026-07-16
supersedes: []
superseded_by: null
---

# MI-003-R2 — Preserved Future Execution Instruction

This file is not execution authorization. It becomes usable only after the
amended contract is committed and pushed unchanged and a later user instruction
supplies the exact published continuity-lab revision, repeats all three exact
bindings, and explicitly authorizes only MI-003-R2 execution. The session
executor is Claude Code operating directly under that instruction.

```text
This is a fresh MI-003-R2 — Bounded Local Work and Scope Segmented Canonical
Complete-Read Correction Retry execution control session only. It authorizes
bounded execution of the unchanged amended contract at continuity-lab revision
<EXACT-PUBLISHED-MI-003-R2-CONTRACT-REVISION>. The session executor is Claude
Code operating directly under this instruction; conducting the contracted work
directly does not constitute invoking Claude Code. It does not authorize
publication, implementation of a selected backlog effort, MI-004, broader work,
nested executors or sub-agents, any Skill or slash command, dependency changes,
external writes, commit, push, or unrelated repository work.

Do not rely on previous chat state.

Before reading repository content, verify Git metadata separately for
continuity-workspace, continuity-lab, and Nestfolio. Require clean main branches
with HEAD, local origin/main, upstream, and remote main exactly aligned at
continuity-workspace eb990e114973e42af9935379f6b13f34f2e5e3b8,
continuity-lab <EXACT-PUBLISHED-MI-003-R2-CONTRACT-REVISION>, and Nestfolio
fd5e58b35e665fa5fa0db3c2c31ea5561442f9b6. Require the continuity-lab revision
to contain the unchanged amended R2 contract, exact subject "Amend MI-003-R2
contract for direct Claude Code execution", and exact single parent
f98d76070949a721e004207e2bc61b4617623ce4. Stop before content read on any
contradiction. Do not fetch, pull, merge, rebase, reset, checkout, restore,
stash, clean, or rewrite history.

After every gate passes, open continuity-lab docs/90-state/program-state.md as
the first logical repository path and keep it the sole logical path addressed
until its complete-read proof closes. Follow canonical_complete_read_protocol
and the exact path order in the R2 Context Pack. For every path, record its
bound blob identity, byte count, logical-line count, and whole-path SHA-256,
then read it completely in contiguous, ordered, forward-only chunks with the
file-read tool. Credit a chunk only when the response presents the requested
range with no truncation, clipping, omission, elision, or persisted-to-file
substitution; anything else is CANONICAL_SEGMENT_INCOMPLETE and may be
recovered only by a forward-contiguous re-read within the same open path
session. Close each path only on gapless full-line coverage plus final byte,
SHA-256, and blob verification. Never batch, parallelize, interleave, reorder,
skip, or substitute a path.

Preserve MI-003 and MI-003-R1 as separate immutable UNCONDITIONAL FAIL results.
Preserve every original and R1 artifact and all MI-002 history byte-for-byte.
Execute only the unchanged MI-003 objective, human-selection boundary,
Nestfolio scope, S1-S10, F1-F12, validation, rollback, prohibitions, and
conjunctive verdict retained by the R2 Context Pack.

First derive the complete deterministic candidate projection and exact
inclusion/exclusion rationale. Do not select silently. If this instruction does
not contain an exact human selection tuple, stop and request exactly one
candidate source path, stable source-local identity, source revision, and
source SHA-256, then verify an exact projection match. Selection authorizes only
the Level 3 representation and route; never perform the selected effort.

Create only the exact twenty-two permitted Nestfolio additions. Keep every
pre-existing tracked byte immutable. Run every contracted success,
mandatory-failure, rebuild, return/cancel, preservation, lower-level regression,
and exact-manifest validation. Use no dependency operation. Do not spawn any
nested executor, sub-agent, Skill, or slash command. Do not create Level 4-6
authority or external writes.

Apply the conjunctive unconditional verdict. After an actual mutation failure,
remove only newly created MI-003 paths, restore and prove exact clean Nestfolio
start state, retain diagnostics outside all repositories, and close only through
the exact R2 continuity-lab execution-closure scope. Do not commit or push.

Report the R2 result, immutable historical disposition, every canonical path
and coverage proof, candidate and human-selection identity, C1-C7, S1-S10,
F1-F12, validation, preservation, rollback, exact manifests, and next valid
operation. No partial or qualified PASS is permitted.
```

The placeholder must be replaced by the exact published amended contract
revision in the later explicit execution instruction. Leaving it unresolved is
a hard stop.
