---
artifact_id: SESSION-PROMPT-MI-003
status: provisional
owner: session
last_updated: 2026-07-15
supersedes: []
superseded_by: null
---

# MI-003 — Preserved Future Execution Instruction

This file is not execution authorization. It becomes usable only after the
finalized contract has been committed and pushed unchanged and a later user
instruction supplies the exact published continuity-lab revision, repeats all
three exact repository bindings, and explicitly authorizes MI-003 execution.

```text
This is a fresh MI-003 — Bounded Local Work and Scope execution control session
only. It authorizes the bounded execution of the unchanged finalized contract at
continuity-lab revision <EXACT-PUBLISHED-MI-003-CONTRACT-REVISION>. It does not
authorize implementation of a selected backlog effort, MI-004, broader work,
Claude Code, any Skill, dependency changes, commit, push, or unrelated repository
work.

Do not rely on previous chat state.

Before reading repository content, verify Git metadata separately for
continuity-workspace, continuity-lab, and Nestfolio. Require continuity-workspace
clean on main at eb990e114973e42af9935379f6b13f34f2e5e3b8, continuity-lab clean
on main at <EXACT-PUBLISHED-MI-003-CONTRACT-REVISION>, and Nestfolio clean on
main at fd5e58b35e665fa5fa0db3c2c31ea5561442f9b6. For every repository require
HEAD, local origin/main, upstream, and remote main exactly aligned. Require the
continuity-lab revision to contain the unchanged finalized MI-003 contract, to
have exact subject "Authorize MI-003 bounded local work and scope contract",
and to be the direct child of 1563cc65b06cacae7557734a08be418cae0ae47a with
exactly the reviewed nine-path delta. Stop before content read on any
contradiction. Do not fetch, pull, merge, rebase, reset, checkout, restore,
stash, clean, or rewrite history.

After the gates pass, follow canonical_read_order in the MI-003 Context Pack
exactly, with docs/90-state/program-state.md first. Treat any deviation as
MI-003 UNCONDITIONAL FAIL and stop before Nestfolio mutation.

Execute only the contract in
sessions/MI-003-bounded-local-work-and-scope/context-pack.yaml. First derive the
complete deterministic candidate projection and inclusion/exclusion rationale.
Do not select silently. If the separate execution instruction does not already
contain an exact human selection tuple, stop and request exactly one candidate
source path, stable source-local identity, source revision, and source SHA-256.
Verify the tuple exactly. Selection authorizes only the Level 3 representation;
do not perform the selected effort.

Create only the exact twenty-two Nestfolio additions permitted by the contract.
Keep every pre-existing tracked byte immutable. Run every contracted success,
mandatory-failure, rebuild, rollback, preservation, lower-level regression, and
manifest validation. Use no dependency operation. Do not invoke Claude Code or
any Skill. Do not create Level 4-6 authority.

Apply the conjunctive unconditional verdict. On actual failure after mutation,
remove only newly created MI-003 paths, restore and prove exact clean Nestfolio
start state, retain diagnostics outside all repositories, and close the result
only through the exact permitted continuity-lab execution-closure scope. Do not
commit or push in this execution session.

Report the exact result, candidate and human-selection identity, C1-C7,
S1-S10, F1-F12, validation, preservation, rollback, exact manifests, and next
valid operation. No partial or qualified PASS is permitted.
```

The placeholder must be replaced with the exact published contract revision in
the later explicit execution instruction. Leaving it unresolved is a hard stop.
