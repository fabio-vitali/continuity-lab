# MI-002 — Reusable Pack Composition — Inactive Future Execution Prompt

This prompt is preserved only as part of the reviewed MI-002 contract. It is
**inactive**. The current contract status is `reviewed-not-authorized`; neither
contract authorization nor execution authorization exists. Do not inspect or
modify Nestfolio, invoke Claude Code or a Skill, execute MI-002, commit, or push.

Commit or push of this prompt would not activate it. It may be used only after a
separate explicit user instruction authorizes MI-002 and a separately authorized
control operation has:

1. verified, using Git metadata before repository-content reads, that
   continuity-lab and Nestfolio are clean, pushed, and remote-aligned on the
   exact revisions recorded in the updated contract;
2. read canonical Program State first and confirmed that MI-002 alone is
   authorized-not-started;
3. set `contract_authorized: true`, `execution_authorized: true`, and the exact
   publication bindings without changing the selected Level 2 scope;
4. preserved every MI-001 through MI-001E-R1 historical result and the exact
   retained `nestfolio.level-1@1.0.1` baseline; and
5. issued a separate execution instruction in a fresh control session.

If and only if all of those conditions are satisfied, the future executor must
follow the complete `context-pack.yaml`, not this summary alone. It must compose
exactly `nestfolio.level-1@1.0.1` with
`continuity.repository-tools@1.0.0`, add only
`continuity.repository-status@1.0.0`, use exact local locked sources with zero
overrides and zero registries, exercise every success and mandatory-failure
scenario, and issue one unconditional PASS or FAIL.

The future execution must remain inside Level 2. It must not change the retained
Level 1 Pack, Procedure, 19 behavior assets, tests, or historical evidence; move
Nestfolio semantics into reusable code or Core; create Work, Scope, Context,
Run, Assurance, completion, Decision, or Learning state; execute a backlog item;
add a remote registry, dependency solver, signing service, marketplace, Console,
hosted service, or external write integration; install dependencies; or modify
any path outside the contract's later-authorized maximum scope.

Any missing authorization, unresolved revision, dirty or unpushed repository,
authority contradiction, scope ambiguity, or retained-baseline mismatch stops
before execution and produces no MI-002 result.
